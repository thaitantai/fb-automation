# API Server Initialization

- [x] Check existing `apps` folder for API server package
- [x] Initialize `apps/api` (if not exists) and set up `package.json`
- [x] Set up standard directory structure (controllers, routes, config)
- [x] Create smart Axios configuration (`config/axios.ts`)
- [x] Create basic Express app entry point (`src/index.ts` and `src/app.ts`)
- [x] Update building scripts and dependencies
